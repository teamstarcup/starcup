Note that the "Grass" and "Stone" folders here are not .rsi directories. They're made after the upstream grass directory, which prefers to link direct paths to the .png files in the prototype rather than use json.